#include<bits/stdc++.h>
// no need to add bunch of libraries one by one we can import only one lbrary for all.
using namespace std;

int main(){
    
    cout << floor(4.5); // we can use math library fun without importing it because of this "#include<bits/stdc++.h>"
    cout << ceil(4.5);
    return 0;
}