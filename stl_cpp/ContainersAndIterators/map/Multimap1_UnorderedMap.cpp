#include<bits/stdc++.h>

int main(){

    void multimap1{

        // allow duplicate keys on sorted order
        // only mapp[key] cannot be used here
    }

    void unorderedmap1{
        // it will randomise not sorted
        // unique keys
        // O(1)
    }
    return 0;
}